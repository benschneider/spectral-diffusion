data:
  channels: 3
  download: true
  height: 32
  root: data
  source: cifar10
  width: 32
diffusion:
  beta_schedule: cosine
  loss_threshold: null
  num_timesteps: 1000
  prediction_type: eps
  time_embed_dim: 128
  fft_norm: ortho
  snr_ratio: 1.0
  spectral_operator_mode: none
evaluation:
  image_size: null
  reference_dir: null
  use_fid: false
loss:
  reduction: mean
model:
  base_channels: 32
  channels: 3
  depth: 2
  type: unet_tiny
optim:
  lr: 0.0001
  weight_decay: 0.0
sampling:
  enabled: false
  num_samples: 16
  num_steps: 1000
  sampler_type: ddim
spectral:
  operator_mode: none
  snr_ratio: 1.0
training:
  batch_size: 32
  epochs: 1
  log_every: 50
  num_batches: 200
