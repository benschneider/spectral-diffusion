# Spectral Diffusion Refactor Recommendations

| Refactor Level | Target | Action | Expected Gain |
| --- | --- | --- | --- |
| 🧱 Deep | `src/reporting/generate_markdown.py` | Split caption heuristics, file crawling, and document assembly into dedicated modules/functions; introduce data classes for figure metadata to cut the 500+ LOC procedural script into composable units.【F:src/reporting/generate_markdown.py†L1-L120】 | Reduces 140-point complexity hotspot, improves testability of report generation, and allows incremental formatting upgrades. |
| 🧱 Deep | `src/training/pipeline.py` | Extract noise preparation, step execution, diagnostics capture, and Taguchi aggregation into smaller services injected into the pipeline; move plotting to a separate visualization utility.【F:src/training/pipeline.py†L63-L200】 | Lowers cognitive load in the training loop, enables unit testing of individual phases, and prepares the ground for multi-experiment orchestration. |
| 🔧 Moderate | CLI + pipeline (`src/cli/train.py`, `src/training/pipeline.py`, `scripts/debug/record_training_steps.py`) | Consolidate duplicated training-step logic by letting debug scripts reuse pipeline subroutines (e.g., step iterator, FFT statistics) instead of reimplementing them.【F:src/training/pipeline.py†L115-L200】【F:scripts/debug/record_training_steps.py†L133-L200】 | Prevents drift between mainline training and diagnostics, reducing maintenance cost for debugging utilities. |
| 🔧 Moderate | Configuration handling (`src/cli/train.py`, `src/cli/common.py`, `configs/`) | Define a single configuration schema/validator layer that merges defaults, CLI overrides, and Taguchi variants; surface structured config objects downstream instead of nested dicts.【F:src/cli/train.py†L102-L199】【F:src/cli/common.py†L18-L195】 | Increases reproducibility, catches invalid runs early, and simplifies downstream consumption. |
| 🔧 Moderate | Spectral utilities (`src/spectral/fft_adapter.py`, `src/spectral/fft_utils.py`) | Harmonize FFT normalization choices and expose a shared configuration object so transforms, noise injection, and inverse transforms use the same conventions.【F:src/spectral/fft_adapter.py†L82-L210】【F:src/spectral/fft_utils.py†L6-L26】 | Eliminates subtle normalization mismatches that can skew spectral energy and complicate comparisons across experiments. |
| 🔧 Moderate | Data builders (`src/training/builders.py`) | Introduce dataset registry & config-driven factory to decouple CIFAR/synthetic logic, and allow plug-in datasets without editing the builder.【F:src/training/builders.py†L16-L200】 | Improves extensibility for new datasets and keeps training pipeline agnostic to data sources. |
| 🔧 Moderate | Taguchi tooling (`src/analysis/taguchi_stats.py`) | Separate file resolution, factor extraction, and statistics computation; add caching for repeated config reads during large sweeps.【F:src/analysis/taguchi_stats.py†L27-L182】 | Speeds up report generation and makes statistical routines reusable for new design-of-experiments workflows. |
| 🪛 Quick Fix | CLI docs/tests (`src/cli/train.py`, `tests/test_training_pipeline.py`) | Expand docstrings and invariants for training entry points; add regression tests covering CLI flag overrides and JSON logging output.【F:src/cli/train.py†L25-L199】【F:tests/test_training_pipeline.py†L1-L120】 | Clarifies expected behaviour for contributors and prevents accidental regressions in orchestration glue. |
| 🪛 Quick Fix | Scripts directory | Replace shell wrappers that duplicate Python orchestration with thin invocations of reusable modules; document remaining scripts with purpose and dependency notes.【F:scripts/debug/record_training_steps.py†L1-L200】 | Keeps auxiliary tooling aligned with core APIs and reduces drift during future refactors. |
